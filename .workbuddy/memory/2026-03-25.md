# 2026-03-25 工作日志

## 埋点需求文档 - 双端拆分

- `埋点需求清单-Delta_Force工具站.xlsx` — 双端拆分版
  - 原来单个总表（30条Both事件）拆为两个独立 Sheet：Desktop端（29条）+ Mobile端（29条）
  - 新增「双端差异对照表」Sheet（13项差异点对照）
  - 序号编码规则：`模块-D序号`（Desktop）/ `模块-M序号`（Mobile）
  - 去掉了"适用端"列（因为已经按端拆分到独立Sheet）
  - 每端各自体现功能差异：如 Desktop 改枪有 source 区分首页/独立页，Mobile 制造有 source 区分首页/工具Tab
  - 大红展馆在 Desktop 为海报分享，在 Mobile 为物品浏览（改名大红藏馆）
  - 分享渠道双端不同：Desktop 含 Facebook/Reddit，Mobile 含 copy_link
  - 保留 Sheet：通用参数说明、事件分层说明、统计汇总（按双端分列）

- `埋点需求文档-Delta_Force工具站.md` — v1.2（双端拆分版）
  - 文档版本从 v1.1 更新到 v1.2
  - 第三章改为按端区分的模块概览：3.1 Desktop端 + 3.2 Mobile端 + 3.3 双端关键差异表
  - 合并原第六章和第七章为新的「上报策略与注意事项」
  - "两端统一上报"改为"双端独立上报"策略

## 事件分层简化：四层 → 两类

- 用户提议将 L1/L2/L3/L4 四层事件分类简化为「曝光」+「点击」两类
- 映射规则：
  - L1(pv_) + L2(expose_) → 曝光(exp_)
  - L3(click_) + L4(result_) → 点击(click_)
- 原 result_ 事件的结果状态改为通过 success 参数传递
  - `result_login` → `click_login_submit` (success: Boolean)
  - `result_logout` → `click_logout`
  - `result_feedback_submit` 合并到 `click_feedback_submit` (新增 success 参数)
- 意见反馈模块从 2 条合并为 1 条，各端事件数从 29 → 28
- Excel：事件层级列从「L1-页面访问/L2-模块曝光/L3-用户交互/L4-业务结果」改为「曝光/点击」
- Excel：事件分层说明 Sheet 改名为「事件分类说明」，仅含两行
- md 文档版本从 v1.2 更新到 v1.3
- md 1.2 章节改为「事件分类」，1.3 命名规范更新前缀说明
- md 第四章漏斗描述加入了具体事件名
- md 第五章指标看板计算方式改用具体事件名表达

## Excel 第四次规整（基于用户手动修改）

- 用户直接修改了 Excel Desktop 端清单，需要基于它重新规整全文件
- 关键改动：
  - 改枪推荐：新增 `exp_gun_builds_loadout`（方案曝光）和 `exp_gun_builds_tab`（枪械tab曝光），Desktop 共 4 条
  - 大红展馆：拆出 `exp_collection_poster_modal`（海报弹窗曝光），Desktop 共 3 条
  - 日报模块：Desktop 删除了 tab 切换事件，保留 2 条
  - 制造模块：Desktop 删除了 tab 切换事件，保留 2 条
  - 意见反馈：Desktop 端移除，仅 Mobile 端保留
  - 参数规范统一：`is_success: 1/0`（数字型），`game_mode: sol/mp`（双端一致）
  - profile_tab 值英文化：overview / record / weapon / operator / map
- Mobile 端基于 Desktop 关注点适配：
  - 新增 `exp_gun_builds_loadout`（方案曝光），共 4 条改枪事件
  - game_mode 值统一为 sol / mp（之前 Mobile 用 fenghuo/zhanchang）
  - 制造模块参数 item_id/department_name 与 Desktop 对齐
  - profile_tab 值英文化：record / collection
- 双端差异对照表更新至 18 项（新增枪械tab曝光差异、制造tab切换差异、参数规范等）
- 最终数量：Desktop 27 条（曝光12+点击15），Mobile 29 条（曝光10+点击19）

## 战绩列表埋点补充建议

- 用户指出"我的数据"页战绩列表相关埋点漏掉，要求补充（不改Excel，仅对话输出）
- 基于原型梳理出 7 条新事件建议：
  - `click_match_filter`（双端）：地图/模式筛选
  - `click_net_profit_toggle`（双端）：净收益开关
  - `click_match_detail`（双端）：点击对局进入详情
  - `exp_match_detail`（仅Desktop）：详情弹窗曝光
  - `click_match_share`（仅Desktop）：详情内分享按钮
  - `click_view_all_matches`（仅Desktop）：总览跳转战绩列表
  - `click_battle_mode_switch`（仅Mobile）：战绩内模式切换
- Desktop 新增 6 条，Mobile 新增 4 条
- 待用户确认后再落地到 Excel

## 埋点需求清单1 格式规整 + 双端差异分析

- 用户新建了 `埋点需求清单1-Delta_Force工具站.xlsx`，包含 3 个 Sheet：Desktop / Mobile / 通用参数
- 格式规整：
  - 补齐所有缺失序号（Desktop: A-D01~L-D02，Mobile: A-M01~L-M02）
  - 补齐缺失的模块名（向上继承）和事件名（`click_login_dismiss` / `click_login_entry`）
  - 删除空行（Mobile Row4/Row16）
  - 统一样式（表头、模块色带、核心指标高亮、自动筛选、冻结首行）
  - 最终：Desktop 33 条，Mobile 33 条
- 逐条双端差异分析完成，识别 8 个关注点：
  1. `method` vs `login_method` 参数名不一致
  2. `target_page_id` vs `target_tab` 参数名不一致
  3. Desktop 缺 `click_craft_main_tab`
  4. Mobile 缺 `click_match_detail`
  5. `click_craft_item` Desktop=hover / Mobile=click 交互差异
  6. `click_feedback_submit` Desktop 缺 `has_text` 参数
  7. 分享渠道枚举双端不同
  8. 多处描述详略不一
- 待用户确认哪些要改
