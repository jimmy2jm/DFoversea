# 用户反馈功能需求文档

> **产品名称**：Delta Force 海外版工具站  
> **模块**：用户反馈（User Feedback）  
> **版本**：V1.0  
> **文档日期**：2026-03-12  
> **作者**：数据产品

---

## 一、功能概述

为工具站用户提供轻量化的反馈入口，支持**选择反馈类型 + 填写文字描述 + 上传截图**，帮助运营团队快速收集和定位用户问题。同时提供管理端后台，支持反馈数据的查看、筛选与批量导出。

---

## 二、用户端

### 2.1 入口

| 端 | 入口位置 | 触发方式 |
|----|---------|---------|
| Desktop | 顶部导航栏 → 用户头像下拉菜单 →「意见反馈」 | 点击后弹出反馈弹窗 |
| Mobile | 顶部 Header → 用户头像下拉菜单 →「意见反馈」 | 点击后弹出反馈弹窗 |

> **前置条件**：用户需登录后才能提交反馈（未登录状态下「意见反馈」入口不可见或点击后引导登录）。

### 2.2 反馈弹窗交互说明

弹窗内自上而下排列以下元素：

#### ① 反馈类型选择（新增）

| 字段 | 说明 |
|------|------|
| 组件形式 | 单选标签组（Tag Radio），横向排列，选中态高亮 |
| 选项列表 | `功能建议` / `BUG反馈` / `数据异常` / `其他` |
| 默认值 | 不默认选中，**必填** |
| 多语言 Key | `feedback.type.suggestion` / `feedback.type.bug` / `feedback.type.data_error` / `feedback.type.other` |

#### ② 反馈内容输入

| 字段 | 说明 |
|------|------|
| 组件形式 | 多行文本框（Textarea） |
| Placeholder | "请描述您遇到的问题或建议…"（多语言 Key: `feedback.placeholder`） |
| 字数限制 | 最多 500 字，右下角实时显示「已输入/500」 |
| 必填校验 | **必填**，不可为空提交 |

#### ③ 图片上传

| 字段 | 说明 |
|------|------|
| 组件形式 | 图片网格 + 添加按钮（"+"） |
| 数量限制 | 最多 **3 张** |
| 大小限制 | 单张不超过 **5 MB** |
| 格式限制 | JPG / PNG / GIF / WEBP |
| 交互细节 | 已上传图片右上角显示删除按钮（×）；点击已上传图片可预览大图 |
| 非必填 | 图片上传为选填 |

#### ④ 提交按钮

| 字段 | 说明 |
|------|------|
| 按钮文案 | "提交反馈"（多语言 Key: `feedback.submit`） |
| 校验逻辑 | 反馈类型 + 反馈内容均已填写 → 按钮可点击；否则按钮置灰不可点击 |
| 提交中状态 | 按钮显示 Loading，防止重复提交 |
| 提交成功 | 弹出 Toast 提示"提交成功，感谢您的反馈！"，1.5s 后自动关闭弹窗 |
| 提交失败 | 弹出 Toast 提示"提交失败，请稍后重试"，弹窗不关闭，用户可重新提交 |

### 2.3 提交数据结构

每条反馈提交时，前端需向后端发送以下字段：

| 字段名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| `feedback_type` | String | ✅ | 反馈类型枚举：`suggestion` / `bug` / `data_error` / `other` |
| `content` | String | ✅ | 反馈文字内容，最多 500 字 |
| `images` | Array\<File\> | ❌ | 上传的图片文件，最多 3 张 |
| `platform` | String | ✅（自动） | 提交端：`desktop` / `mobile` |
| `language` | String | ✅（自动） | 当前语言环境：`zh-CN` / `en` / `ja` / `ko` / `de` |
| `user_id` | String | ✅（自动） | 当前登录用户 ID |
| `user_agent` | String | ✅（自动） | 浏览器 UA 信息 |
| `page_url` | String | ✅（自动） | 提交反馈时所在页面 URL |
| `timestamp` | Long | ✅（自动） | 提交时间戳（毫秒） |

> 标注"自动"的字段由前端自动采集，用户无需手动填写。

### 2.4 用户端交互流程图

```
用户点击「意见反馈」
       │
       ▼
  弹出反馈弹窗
       │
       ▼
  ┌─────────────────┐
  │ ① 选择反馈类型   │  ← 必填
  │ ② 填写反馈内容   │  ← 必填（≤500字）
  │ ③ 上传图片(可选)  │  ← 最多3张，≤5MB/张
  └─────────────────┘
       │
       ▼
  点击「提交反馈」
       │
       ├── 校验失败 → 提示缺失项（类型/内容未填）
       │
       ├── 提交中 → Loading 防重复
       │
       ├── 成功 → Toast "提交成功" → 自动关闭弹窗
       │
       └── 失败 → Toast "提交失败" → 保留弹窗可重试
```

---

## 三、管理端

### 3.1 功能概述

提供 Web 后台管理页面，供运营/产品人员查看和管理用户提交的反馈数据。

### 3.2 反馈列表页

#### 页面布局

```
┌──────────────────────────────────────────────────────────┐
│  筛选栏                                                   │
│  [日期范围选择器]  [反馈类型▼]  [提交端▼]  [语言▼]  [查询] │
├──────────────────────────────────────────────────────────┤
│  操作栏                                                   │
│  [✅ 全选]  已选 0 条    [📥 批量导出]                      │
├──────────────────────────────────────────────────────────┤
│  反馈列表（表格）                                          │
│  □  | 编号 | 类型 | 内容摘要 | 图片 | 提交端 | 语言 | ... │
│  □  | F001 | BUG  | 改枪页面... | 2张 | Desktop | en | ... │
│  □  | F002 | 建议  | 希望增加... | 0张 | Mobile  | ja | ... │
│  ...                                                      │
├──────────────────────────────────────────────────────────┤
│  分页栏    < 1 2 3 ... 10 >     每页显示 [20▼] 条          │
└──────────────────────────────────────────────────────────┘
```

#### 筛选条件

| 筛选项 | 组件形式 | 选项 | 说明 |
|--------|---------|------|------|
| 日期范围 | 日期范围选择器（DateRangePicker） | 自定义起止日期 | 按反馈提交时间筛选，默认近 7 天 |
| 反馈类型 | 下拉单选 | `全部` / `功能建议` / `BUG反馈` / `数据异常` / `其他` | 默认"全部" |
| 提交端 | 下拉单选 | `全部` / `Desktop` / `Mobile` | 默认"全部" |
| 语言 | 下拉单选 | `全部` / `zh-CN` / `en` / `ja` / `ko` / `de` | 默认"全部" |

点击「查询」按钮后刷新列表结果。

#### 列表字段

| 列名 | 说明 |
|------|------|
| 勾选框 | 用于批量操作选中 |
| 编号 | 反馈唯一 ID，自增，格式如 `F0001` |
| 反馈类型 | 标签形式展示：功能建议(蓝) / BUG反馈(红) / 数据异常(橙) / 其他(灰) |
| 内容摘要 | 截取前 50 字 + "…"，鼠标 Hover 显示完整内容 Tooltip |
| 图片数 | 显示上传图片张数，如 `2张`，点击可预览 |
| 提交端 | `Desktop` / `Mobile` |
| 语言 | 用户提交时的语言环境 |
| 用户ID | 提交用户标识 |
| 提交时间 | 格式：`YYYY-MM-DD HH:mm:ss` |

- 默认按提交时间**倒序**排列（最新在前）
- 每页显示 **20** 条，支持切换为 20 / 50 / 100

### 3.3 反馈详情

点击列表中某条反馈，展开或跳转至详情视图，展示完整信息：

| 字段 | 展示内容 |
|------|---------|
| 反馈编号 | F0001 |
| 反馈类型 | 功能建议 / BUG反馈 / 数据异常 / 其他 |
| 反馈内容 | 完整文字（支持换行） |
| 上传图片 | 缩略图列表，点击可放大预览 |
| 提交端 | Desktop / Mobile |
| 语言环境 | zh-CN / en / ja / ko / de |
| 用户ID | xxx |
| 浏览器UA | 完整 User-Agent 字符串 |
| 提交页面 | 用户提交时所在 URL |
| 提交时间 | YYYY-MM-DD HH:mm:ss |

### 3.4 批量导出

| 项目 | 说明 |
|------|------|
| 触发方式 | 勾选一条或多条反馈后，点击「批量导出」按钮 |
| 未勾选行为 | 若未勾选任何条目，点击「批量导出」时默认导出**当前筛选条件下的全部数据** |
| 导出格式 | Excel（.xlsx） |
| 文件命名 | `用户反馈_YYYYMMDD_HHmmss.xlsx` |
| 导出字段 | 编号、反馈类型、反馈内容（完整）、图片链接、提交端、语言、用户ID、浏览器UA、提交页面、提交时间 |
| 图片处理 | 导出时图片字段为图片的 CDN 链接（多张用换行分隔），不导出图片文件本身 |
| 数据上限 | 单次导出不超过 **10,000** 条，超出时提示"数据量过大，请缩小筛选范围" |

---

## 四、非功能性要求

| 项目 | 要求 |
|------|------|
| 多语言 | 用户端反馈弹窗需支持 zh-CN / en / ja / ko / de 五种语言 |
| 图片存储 | 上传图片存至 CDN/对象存储，保留原图 |
| 接口限频 | 同一用户 1 分钟内最多提交 **3 次**反馈，超出提示"操作频繁，请稍后再试" |
| 数据保留 | 反馈数据保留 **180 天**，过期自动归档 |
| 权限控制 | 管理端需登录且具有「反馈管理」权限方可访问 |

---

## 五、接口清单（供开发参考）

| 序号 | 接口名称 | Method | 路径（建议） | 说明 |
|------|---------|--------|-------------|------|
| 1 | 提交反馈 | POST | `/api/feedback/submit` | 用户端提交反馈，Content-Type: multipart/form-data |
| 2 | 反馈列表 | GET | `/api/admin/feedback/list` | 管理端查询列表，支持分页 + 筛选参数 |
| 3 | 反馈详情 | GET | `/api/admin/feedback/{id}` | 管理端查看单条反馈详情 |
| 4 | 批量导出 | POST | `/api/admin/feedback/export` | 管理端导出 Excel，Body 传选中 ID 列表或筛选条件 |

### 接口 1：提交反馈

**Request**（multipart/form-data）

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| feedback_type | string | ✅ | 枚举：suggestion / bug / data_error / other |
| content | string | ✅ | 反馈内容，≤500字 |
| images | file[] | ❌ | 图片文件，最多3张，单张≤5MB |
| platform | string | ✅ | desktop / mobile |
| language | string | ✅ | 语言代码 |
| page_url | string | ✅ | 当前页面URL |

**Response**

```json
{
  "code": 0,
  "message": "success",
  "data": {
    "feedback_id": "F0001"
  }
}
```

### 接口 2：反馈列表

**Request**（Query Parameters）

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| start_date | string | ❌ | 起始日期 YYYY-MM-DD，默认7天前 |
| end_date | string | ❌ | 结束日期 YYYY-MM-DD，默认今天 |
| feedback_type | string | ❌ | 筛选类型 |
| platform | string | ❌ | 筛选端 |
| language | string | ❌ | 筛选语言 |
| page | int | ❌ | 页码，默认1 |
| page_size | int | ❌ | 每页条数，默认20 |

**Response**

```json
{
  "code": 0,
  "data": {
    "total": 156,
    "page": 1,
    "page_size": 20,
    "list": [
      {
        "feedback_id": "F0001",
        "feedback_type": "bug",
        "content": "改枪推荐页面加载缓慢...",
        "image_count": 2,
        "image_urls": ["https://cdn.../img1.jpg", "https://cdn.../img2.jpg"],
        "platform": "desktop",
        "language": "en",
        "user_id": "u_123456",
        "user_agent": "Mozilla/5.0...",
        "page_url": "https://tool.deltaforce.com/gun-builds",
        "created_at": "2026-03-12 14:30:22"
      }
    ]
  }
}
```

### 接口 4：批量导出

**Request**（JSON Body）

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| feedback_ids | string[] | ❌ | 指定导出的反馈ID列表；为空则按筛选条件导出全部 |
| start_date | string | ❌ | 筛选起始日期 |
| end_date | string | ❌ | 筛选结束日期 |
| feedback_type | string | ❌ | 筛选类型 |
| platform | string | ❌ | 筛选端 |
| language | string | ❌ | 筛选语言 |

**Response**：返回 Excel 文件流（Content-Type: application/vnd.openxmlformats-officedocument.spreadsheetml.sheet）

---

## 六、与现有原型的差异说明

当前原型（Desktop / Mobile）的反馈弹窗**缺少「反馈类型」选择**，本需求新增该字段：

| 对比项 | 现有原型 | 本次需求 |
|--------|---------|---------|
| 反馈类型 | ❌ 无 | ✅ 新增：功能建议 / BUG反馈 / 数据异常 / 其他 |
| 文字输入 | ✅ 有（500字限制） | ✅ 保持一致 |
| 图片上传 | ✅ 有（3张/5MB） | ✅ 保持一致 |
| 管理后台 | ❌ 无 | ✅ 新增：列表查看 + 筛选 + 批量导出 |
