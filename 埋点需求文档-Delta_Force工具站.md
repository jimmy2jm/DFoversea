# Delta Force 海外版工具站 — 数据埋点需求文档

> **产品名称**：Delta Force Tracker（三角洲行动海外版工具站）  
> **版本**：V1.0  
> **文档版本**：v1.1（精简版）  
> **撰写日期**：2026-03-12  
> **更新日期**：2026-03-13  
> **撰写人**：数据产品经理  
> **适用端**：Desktop (PC Web) + Mobile (H5)

---

## 一、文档说明

### 1.1 目的

本文档为 Delta Force 海外版工具站 V1.0 的数据埋点需求框架，提供给前后端开发团队实施埋点。文档覆盖两端（Desktop / Mobile）所有可见功能模块的用户交互行为，不涉及 V1.0 注释隐藏的功能（Wiki 页、资产 Tab、地图工具、交易物价模块等）。

> 💡 **具体埋点事件清单**请参见配套文件：`埋点需求清单-Delta_Force工具站.xlsx`

### 1.2 事件分层体系

| 层级 | 类型 | 命名前缀 | 说明 | 示例 |
|------|------|---------|------|------|
| L1 | 页面访问（Page View） | `pv_` | 用户进入某个页面/Tab | `pv_home`, `pv_my_data` |
| L2 | 模块曝光（Module Expose） | `expose_` | 页面内某功能模块进入可视区域 | `expose_daily_report` |
| L3 | 用户交互（User Action） | `click_` | 点击、滑动、切换、输入等主动行为 | `click_login_entry` |
| L4 | 业务结果（Biz Result） | `result_` | 关键业务流程的结果反馈 | `result_login` |

### 1.3 命名规范

**事件名格式**：`{层级前缀}_{模块}_{动作}`

- 层级前缀：`pv_` / `expose_` / `click_` / `result_`
- 模块名：小写英文，下划线分隔（如 `daily_report`, `gun_builds`, `craft`）
- 动作：动词或动词短语（如 `view`, `click`, `switch`, `copy`, `share`, `submit`）

### 1.4 通用参数（每条事件默认携带）

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `platform` | String | 端类型：`desktop` / `mobile` |
| `page_id` | String | 当前页面标识 |
| `user_id` | String | 用户ID（未登录传空） |
| `login_status` | String | `logged_in` / `guest` |
| `login_method` | String | 登录平台：`level_infinite` / `garena`（未登录传空） |
| `language` | String | 当前语言：`zh-CN` / `en` / `ja` / `ko` / `de` |
| `timestamp` | Long | 事件发生时间戳 |
| `session_id` | String | 会话ID |

---

## 二、页面结构总览

### Desktop 端（多页面架构）

| 页面 | 文件 | page_id | 说明 |
|------|------|---------|------|
| 首页 | `index.html` | `home` | 日报 + 密码 + 改枪推荐 + 制造 + 大红展馆 |
| 我的数据 | `my-data.html` | `my_data` | 个人资料 + 战绩数据（烽火/战场双模式） |
| 改枪推荐（独立页） | `gun-builds.html` | `gun_builds` | 枪械列表 + 改装方案（V1.0 导航隐藏但页面存在） |
| 交易制造（独立页） | `craft.html` | `craft_page` | 制造推荐 + 交易物价（V1.0 导航隐藏但页面存在） |

### Mobile 端（单页面架构，Tab 切换）

| 页面 | page_id | 说明 |
|------|---------|------|
| 首页 Tab | `m_home` | 日报 + 密码 + 收益 + 制造 + 改枪推荐 |
| 工具 Tab | `m_tools` | 制造推荐（交易物价 V1.0 隐藏） |
| 我的 Tab | `m_profile` | 个人资料 + 战绩 + 大红藏馆 |
| 物价详情子页面 | `m_price_detail` | 物品物价详情（从工具页点入） |

---

## 三、埋点模块概览与数据分析目标

> 各模块的具体埋点事件清单详见 Excel 文件，此处仅列出模块划分与分析目标。

### 模块 A：登录与账号（6 条事件）

| 分析目标 | 说明 |
|---------|------|
| 登录转化率 | 从点击登录入口到登录成功的整体转化率 |
| 登录平台偏好 | Level Infinite vs Garena 的选择分布 |
| 登录方式分布 | 邮箱登录 vs 第三方社交登录的占比 |

### 模块 B：导航与页面（4 条事件）

| 分析目标 | 说明 |
|---------|------|
| 页面热度分布 | 各页面 PV / UV 排行 |
| 导航使用率 | 顶部导航（Desktop）/ 底部 Tab（Mobile）各入口的点击热度 |
| 语言偏好 | 用户语言切换行为及各语言区的活跃分布 |

### 模块 C：日报模块（3 条事件）

| 分析目标 | 说明 |
|---------|------|
| 模式偏好 | 烽火地带 vs 全面战场日报的查看占比 |
| 分享意愿 | 日报分享按钮的触达率 |

### 模块 D：今日密码（2 条事件）

| 分析目标 | 说明 |
|---------|------|
| 密码使用热度 | 各地图密码的复制次数排行 |

### 模块 E：改枪推荐（3 条事件）

| 分析目标 | 说明 |
|---------|------|
| 枪械关注度排行 | 各枪械的选择/查看次数排行 |
| **改枪码复制次数**（核心指标） | 各枪械 × 方案类型（性价比 / 满改）的复制热度 |

### 模块 F：特勤处制造（3 条事件）

| 分析目标 | 说明 |
|---------|------|
| 内容偏好 | 制造详情 vs 制造推荐两个 Tab 的使用比例 |
| 物品关注度排行 | 推荐物品的点击热度，关注高收益物品偏好 |

### 模块 G：大红展馆（2 条事件）

| 分析目标 | 说明 |
|---------|------|
| 海报分享 | 海报生成触达率及各分享渠道的使用分布 |

### 模块 H：分享功能（2 条事件）

| 分析目标 | 说明 |
|---------|------|
| **分享渠道转化率**（核心指标） | 各分享渠道的使用占比与成功率 |

### 模块 K：我的页面（3 条事件）

| 分析目标 | 说明 |
|---------|------|
| 模式偏好 | 烽火地带 vs 全面战场的切换频次 |
| 数据维度关注度 | 总览 / 战绩 / 枪械 / 干员 / 地图各 Tab 的查看分布 |

### 模块 L：意见反馈（2 条事件）

| 分析目标 | 说明 |
|---------|------|
| 反馈提交率 | 提交率、文字+图片完整度 |

---

## 四、关键用户路径（核心漏斗）

### 4.1 登录转化漏斗

```
首页 PV → 点击登录入口 → 选择登录方式 → 进入登录弹窗 → 提交登录 → 登录成功
```

### 4.2 改枪码使用漏斗

```
改枪推荐模块曝光 → 选择枪械 → 查看改装方案 → 复制改枪码
```

### 4.3 分享漏斗

```
日报曝光 → 点击分享按钮 → 海报弹窗打开 → 选择分享渠道 → 分享成功
```

### 4.4 制造推荐使用漏斗

```
制造模块曝光 → 切换 Tab → 选中物品 → 查看价格走势图
```

---

## 五、核心指标看板建议

| 指标 | 计算方式 | 关注维度 |
|------|----------|----------|
| DAU / MAU | 每日/每月独立访客数 | 整体、按端拆分 |
| 登录率 | 登录成功用户 / 总 UV | 按登录方式、社交渠道拆分 |
| 改枪码复制次数 | 复制事件总数 | 按枪械、方案类型拆分 |
| 日报分享率 | 分享成功数 / 日报曝光数 | 按分享渠道拆分 |
| 密码功能使用率 | 密码复制 / 指引打开数 / 模块曝光数 | 按地图拆分 |
| 制造模块互动深度 | 物品点击数 / 模块曝光数 | 按工作台拆分 |
| 反馈提交率 | 反馈提交成功数 / 反馈弹窗打开数 | — |
| 语言分布 | 按 `language` 参数分布 | — |

---

## 六、Desktop 与 Mobile 差异说明

| 差异点 | Desktop | Mobile |
|--------|---------|--------|
| 页面架构 | 多页面跳转（index / my-data / gun-builds / craft） | 单页面 Tab 切换（home / tools / profile） |
| 日报分享渠道 | 保存图片、X、Discord、Facebook、Reddit | 保存图片、Discord、X、复制链接 |
| 大红藏馆 | 首页独立模块（含海报分享） | 我的页面内展示 |
| 退出登录 | 直接退出 | 有确认弹窗（取消 / 确定） |
| 密码交互 | 直接展示密码 + 密码房指引按钮 | 点击密码卡片打开指引弹窗 |
| 制造交互 | 鼠标 Hover Tooltip | 点击打开底部抽屉弹窗 |
| 改枪推荐 | 首页内嵌 + 独立页（含枪械列表） | 仅首页内嵌 |

---

## 七、注意事项

1. **V1.0 不需要埋点的隐藏功能**：Desktop 被注释的导航链接、Mobile 隐藏的 Wiki Tab、交易物价模块（`display:none`）、资产 Tab。

2. **两端统一上报**：同一交互使用相同事件名，通过 `platform` 参数区分端。

3. **曝光事件去重**：同一会话内同一模块重复进入可视区域，仅上报首次曝光。

4. **登录态处理**：`user_id` 未登录时传空字符串，登录后补传。

5. **语言参数**：所有事件均携带当前界面语言，用于分析不同语言区用户行为差异。

---

> 📎 **配套文件**：
> - `埋点需求清单-Delta_Force工具站.xlsx` — 完整事件清单（开发实施用）
> - `埋点参考材料汇编.md` — 行业埋点规范与参考链接
