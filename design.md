# Delta Force 工具站 — 内容样式规范指引

> 基于正式上线版本提炼，后续所有 UI 修改必须遵照本指引，确保视觉一致性。

---

## 1. 设计理念

| 关键词 | 说明 |
|--------|------|
| **战术硬核** | 直角切边、冷峻配色、无装饰性圆角，传递军事工具感 |
| **信息密度** | 紧凑间距、数据优先、减少留白浪费 |
| **深色沉浸** | 深青底色 + 绿青高亮，降低视觉疲劳，突出数据 |

---

## 2. 配色体系

### 2.1 主色调

| 用途 | PC 端 | 移动端 | 备注 |
|------|-------|--------|------|
| **品牌绿/青（主强调色）** | `#24f4b2` | `#00d4aa` | 导航激活、数据高亮、CTA 按钮填充 |
| 品牌绿 hover/浅色 | `#78ffd8` | — | 仅 PC hover 态 |
| 品牌绿暗底 | `rgba(36, 244, 178, 0.16)` | `rgba(0, 212, 170, 0.06~0.15)` | 激活态背景、幽灵按钮 |
| 品牌绿边框 | `rgba(36, 244, 178, 0.35~0.5)` | `rgba(0, 212, 170, 0.3~0.42)` | 模式切换容器边框、按钮边框 |
| 品牌绿发光 | `0 0 12px rgba(36, 244, 178, 0.24)` | `0 0 10px rgba(0, 212, 170, 0.2~0.25)` | 关键数值 text-shadow |

### 2.2 背景层级

| 层级 | 色值 | 用途 |
|------|------|------|
| **页面底色** | `#0b2022 → #071c1e → #07191a`（纵向渐变） | body 背景 |
| 顶部径向光晕 | `radial-gradient(circle at 64% 0%, rgba(126, 146, 139, 0.22) 0, transparent 30%)` | body 第一层叠加 |
| **导航/底栏** | `rgba(4, 18, 20, 0.94~0.96)` + `backdrop-filter: blur(8px)` | 顶栏、移动端底栏 |
| **面板主卡片** | `rgba(18, 38, 42, 0.9~0.92)` | 日报卡片、工具面板、模块卡片 |
| **面板头部** | `rgba(92, 112, 114, 0.45~0.52)` | section-header、sub-card-header |
| **内嵌子元素** | `rgba(6, 18, 20, 0.66)` | item-card、build-card、stat-item |
| **Tab 容器** | `rgba(10, 32, 35, 0.72~0.84)` | report-tabs-inner、gun-mode-tabs |

### 2.3 边框

| 用途 | 色值 |
|------|------|
| 面板主边框 | `rgba(84, 112, 118, 0.48~0.56)` |
| 子元素边框 | `rgba(42, 72, 78, 0.75)` |
| 分隔线（轻） | `rgba(84, 112, 118, 0.28~0.35)` |
| 分隔线（header 底部） | `rgba(255, 255, 255, 0.06)` |
| 导航底栏边框 | `rgba(80, 108, 112, 0.45)` |

### 2.4 文字

| 用途 | 色值 |
|------|------|
| 主文字 | `rgba(255, 255, 255, 0.86~0.92)` |
| 次要文字 | `rgba(255, 255, 255, 0.56~0.72)` / `var(--text-secondary)` |
| 强调数值 | `#24f4b2`（PC）/ `#00d4aa`（移动） |
| 面板标题 | `rgba(255, 255, 255, 0.86~0.9)` font-weight: 800 |
| Kicker/标签 | `#24f4b2` letter-spacing: 0.12~0.18em |

### 2.5 辅助色

| 色值 | 用途 |
|------|------|
| `#c8ff00` | 每日关键词主数据（黄绿，仅海报模式） |
| `var(--accent-orange)` | 密码码值、警告信息 |
| `var(--accent-red)` | 错误、下降指标 |
| `var(--accent-green)` | 进度条渐变终点、上涨指标 |

---

## 3. 圆角规范（核心规则）

> **全局原则：border-radius: 0（直角）**

正式版采用军事硬核直角风格，**所有面板、卡片、按钮、Tab、输入框均不使用圆角**。

| 元素 | border-radius | 说明 |
|------|---------------|------|
| 面板卡片 | `0` | 面板容器、日报卡片、工具面板 |
| 按钮/Tab | `0` | 主 Tab、次级 Tab、CTA 按钮 |
| 子卡片 | `0` | item-card、build-card、stat-item |
| 图片占位 | `0` | 枪械图片、物品图片、预览图 |
| 进度条 | `0`（PC）/ `3~4px`（仅进度条内部填充可微圆） | 保持硬朗感 |
| **唯一例外** | 用户头像 `border-radius: 50%` | 圆形头像保留 |

---

## 4. 排版规范

### 4.1 字体栈

| 用途 | 字体 |
|------|------|
| **数据/数字** | `'Orbitron', monospace` | 业绩数值、密码码值、时间倒计时 |
| **正文/标题** | `-apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif` | 面板标题、描述文字 |
| **Logo** | `'Orbitron', sans-serif` | 顶部 Logo 文字 |

### 4.2 字重

| 场景 | font-weight |
|------|-------------|
| 面板标题 | `800` |
| Tab 文字/按钮文字 | `700` |
| 数据数值 | `700~800` |
| 正文描述 | `400` |

### 4.3 字号（PC）

| 场景 | font-size |
|------|-----------|
| 页面大标题 | `1.8rem` |
| 面板标题 | `0.92rem` |
| Tab 文字 | `0.92rem` |
| 正文 | `0.86rem` |
| 小字/标签 | `0.72~0.78rem` |
| 主数据数值 | `2.25rem`（Orbitron） |
| 副数据数值 | `1rem` |

### 4.4 字号（移动端）

| 场景 | font-size |
|------|-----------|
| 面板标题 | `14~15px` |
| Tab 文字 | `12~13px` |
| 正文 | `12px` |
| 小字/标签 | `10~11px` |
| 主数据数值 | `16~18px`（Orbitron） |

### 4.5 letter-spacing

| 场景 | 值 |
|------|-----|
| Logo | `1~2px` |
| Kicker/标签 | `0.12~0.18em` |
| 导航 Tab | `0.04em` |
| 数据数值 | `1px` |

---

## 5. 间距与尺寸

### 5.1 PC 端

| 属性 | 值 |
|------|-----|
| 最大内容宽度 | `1060px` |
| 面板间距 | `14px` |
| 面板内边距 | `14~18px` |
| 导航栏高度 | `54px` |
| 主 Tab 高度 | `34px` |
| 主 Tab 最小宽度 | `118px` |
| 面板头部高度 | `32px` |
| 面板头部内边距 | `6px 12px` |
| 子卡片内边距 | `6~7px 3~5px` |

### 5.2 移动端

| 属性 | 值 |
|------|-----|
| 面板外边距 | `8px` |
| 面板内边距 | `10px` |
| 导航栏/底栏高度 | `44~48px` |
| Tab 高度 | `32px` |
| 面板头部高度 | `32px` |
| 子卡片内边距 | `6~7px 3~5px` |
| 底部 Tab 图标区 | `24px` |

---

## 6. 组件规范

### 6.1 主 Tab（模式切换）

```
容器: border: 1px solid rgba(36, 244, 178, 0.35)
      background: rgba(10, 32, 35, 0.84)
      border-radius: 0
      padding: 0

默认 Tab: color: rgba(255, 255, 255, 0.6)
          background: transparent
          border-radius: 0
          padding: 10px 38px (PC) / 0 8px (移动)

激活 Tab: background: rgba(36, 244, 178, 0.16)
          color: #24f4b2
          (PC 端两侧有三角箭头装饰)
```

### 6.2 导航 Tab（顶部页面切换）

```
默认: background: rgba(255, 255, 255, 0.08)
      border: 1px solid rgba(255, 255, 255, 0.08)
      color: rgba(255, 255, 255, 0.72)
      font-weight: 700
      letter-spacing: 0.04em
      border-radius: 0

激活: background: #24f4b2
      border-color: #24f4b2
      color: #031816
```

### 6.3 面板卡片

```
border-radius: 0
background: rgba(18, 38, 42, 0.9)
border: 1px solid rgba(84, 112, 118, 0.52)
box-shadow: 0 18px 60px rgba(0, 0, 0, 0.18)
```

### 6.4 面板头部

```
min-height: 32px
padding: 6px 12px (PC) / 7px 10px (移动)
background: rgba(92, 112, 114, 0.45~0.52)
border-bottom: 1px solid rgba(255, 255, 255, 0.06)
font-weight: 800
color: rgba(255, 255, 255, 0.9)
```

### 6.5 幽灵按钮（边框按钮）

```
border: 1px solid rgba(36, 244, 178, 0.5)
background: rgba(36, 244, 178, 0.06)
color: #24f4b2
border-radius: 0
font-weight: 700

hover: background: #24f4b2
       color: #031816
```

### 6.6 子卡片（物品/枪械/数据格）

```
border-radius: 0
background: rgba(6, 18, 20, 0.66)
border: 1px solid rgba(42, 72, 78, 0.75)
```

### 6.7 图片占位/预览

```
border-radius: 0
background: linear-gradient(135deg, rgba(74, 92, 96, 0.8), rgba(18, 38, 42, 0.86))
```

### 6.8 行内分隔条（密码/载具条）

```
border-radius: 0
background: rgba(18, 38, 42, 0.9)
border: 1px solid rgba(84, 112, 118, 0.56)
overflow: hidden

标题区: background: rgba(92, 112, 114, 0.52)
        min-width: 118px
分隔: border-left: 1px solid rgba(84, 112, 118, 0.35)（各列间）
```

---

## 7. 动效规范

| 属性 | 值 | 说明 |
|------|-----|------|
| 过渡时间 | `0.2s ease` | hover 态、按钮反馈 |
| 发光效果 | `text-shadow: 0 0 10~12px rgba(36, 244, 178, 0.24)` | 关键数值微光 |
| 模糊背景 | `backdrop-filter: blur(8px)` | 导航栏、底栏 |
| 禁止效果 | — | 禁止弹跳、弹性缩放、大范围位移动画 |

---

## 8. 交互状态

| 状态 | 样式 |
|------|------|
| **hover** | 边框变品牌绿 `border-color: #24f4b2`；背景加微透明品牌绿 `background: rgba(36, 244, 178, 0.05~0.12)` |
| **active/选中** | 品牌绿填充背景 + 深色文字 `background: #24f4b2; color: #031816` |
| **focus** | 输入框 `border-color: var(--accent-cyan)` |
| **disabled** | 降低不透明度 `opacity: 0.5` |

---

## 9. 响应式断点

| 断点 | 说明 |
|------|------|
| `> 1200px` | PC 全宽布局，内容居中 `max-width: 1060px` |
| `≤ 1200px` | PC 紧凑布局，内边距 `18px` |
| `≤ 768px` | 平板/移动端布局 |

---

## 10. 代码约定

### CSS 变量使用

```css
/* PC 端 ::root */
--accent-cyan: #00ffcc;   /* 基础变量（旧版） */
/* 正式版覆盖中直接使用 #24f4b2 硬编码，确保与正式版一致 */

/* 移动端 ::root */
--accent-cyan: #00d4aa;
/* 正式版覆盖中直接使用 #00d4aa 硬编码 */
```

### 样式覆盖分区标记

```css
/* ============================================
   HQ official layout refinement - PC home
   ============================================ */

/* ============================================
   HQ official layout refinement - sub pages
   ============================================ */

/* ============================================
   HQ official layout refinement - mobile home
   ============================================ */
```

所有正式版风格覆盖样式必须放在对应分区注释内，便于维护和回溯。

---

## 11. 禁止清单

| 禁止 | 原因 |
|------|------|
| 圆角（除头像外） | 破坏军事硬核风格 |
| 渐变按钮填充 | 正式版仅使用纯色填充或幽灵按钮 |
| 大面积白色/浅色背景 | 破坏深色沉浸感 |
| 卡片投影过重 | `box-shadow` 不超过 `0 18px 60px rgba(0,0,0,0.18)` |
| 装饰性图标/插画 | 信息优先，减少视觉噪音 |
| 弹性动画/bounce | 与硬核风格冲突 |

---

*文档版本：v1.0 · 基于 2026 年 4 月正式上线版本提炼*
